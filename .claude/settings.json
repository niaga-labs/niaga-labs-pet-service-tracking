{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Project-scoped settings, committed. Global permissions and hooks come from ~/.claude/settings.json; add here only what this repo needs on top (repo scripts, env). Per-machine overrides → settings.local.json (gitignored).",
  "env": {},
  "permissions": {
    "allow": [
      "Bash(bash .claude/scripts/*)",
      "Bash(bash scripts/*)",
      "Bash(make *)"
    ]
  }
}
