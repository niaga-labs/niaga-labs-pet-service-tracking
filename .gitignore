# Binaries
*.exe
*.dll
*.so
*.dylib
/server
/server-linux

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
*.env.local

# OS
.DS_Store
Thumbs.db

# Go
vendor/
/tmp/

# Claude Code. The layer is COMMITTED on purpose -- memory, rules and the
# commands table are shared context, not local scratch. Only per-machine files
# are ignored, below. (KPD-51)

# Claude Code, per-machine
.claude/settings.local.json
CLAUDE.local.md
